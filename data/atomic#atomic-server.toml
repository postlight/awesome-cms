name = "AtomicServer"
description = "AtomicServer is a lightweight and fast open source graph database server and CMS that includes authorization, full-text search and an easy to use API."
url = "https://atomicserver.eu"
github_repo = "atomicdata-dev/atomic-server"
language = "rust"
